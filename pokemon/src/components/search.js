import React from 'react';

const Search = ( { onChange } ) => (
    <input type="text"
        onChange={onCharge}
        placeholder="Enter pokemon name...."
        autoFocus
    />
)

export default Search;
